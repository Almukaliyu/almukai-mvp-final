import { serialize } from 'cookie'; import dotenv from 'dotenv'; dotenv.config(); export default function handler(req,res){ if(req.method!=='POST') return res.status(405).end(); const { user, pass } = req.body; const ADMIN_USER = process.env.ADMIN_USER || 'almuk'; const ADMIN_PASS = process.env.ADMIN_PASS || 'almukai'; if(user===ADMIN_USER && pass===ADMIN_PASS){ res.setHeader('Set-Cookie', serialize('almukai_admin','1',{ path:'/', httpOnly:true })); return res.json({ ok:true }); } res.status(401).json({ error:'Invalid' }); }
