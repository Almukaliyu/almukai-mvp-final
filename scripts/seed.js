const mongoose = require('mongoose'); const User = require('../src/models/User').default; const Mentor = require('../src/models/Mentor').default; require('dotenv').config(); async function run(){ await mongoose.connect(process.env.MONGODB_URI || 'mongodb://127.0.0.1:27017/almukai'); await User.deleteMany({}); await Mentor.deleteMany({}); const u = await User.create({ email:'test@almukai.app', password: require('bcryptjs').hashSync('password123',8), credits:200 }); await Mentor.create({ name:'Zainab Bello', skills:'Career Coaching, Frontend', price:5000, rating:4.8 }); console.log('Seeded'); process.exit(0); } run().catch(e=>{ console.error(e); process.exit(1); });
